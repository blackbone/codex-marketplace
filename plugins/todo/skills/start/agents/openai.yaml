interface:
  display_name: "ToDo Start"
  short_description: "Start the local ToDo background runner"
  default_prompt: "Use $todo:start to start ToDo for this repository."
policy:
  allow_implicit_invocation: false
