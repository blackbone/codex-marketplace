interface:
  display_name: "ToDo Start"
  short_description: "Start runner, monitoring, and dashboard"
  default_prompt: "Use $todo:start to start ToDo, bind monitoring, and open its dashboard."
policy:
  allow_implicit_invocation: false
