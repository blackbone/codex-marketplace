interface:
  display_name: "ToDo Start"
  short_description: "Start runner and get dashboard URL"
  default_prompt: "Use $todo:start to start ToDo and return its dashboard URL."
policy:
  allow_implicit_invocation: false
