interface:
  display_name: "ToDo Artifact Add"
  short_description: "Attach context to an existing unclaimed task"
  default_prompt: "Use $todo:artifact-add to attach context to a ToDo task."
policy:
  allow_implicit_invocation: false
