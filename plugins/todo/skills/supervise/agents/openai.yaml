interface:
  display_name: "ToDo Supervise"
  short_description: "Inspect runner health and task failures"
  default_prompt: "Use $todo:supervise to inspect this repository's runner and failures."
policy:
  allow_implicit_invocation: false
